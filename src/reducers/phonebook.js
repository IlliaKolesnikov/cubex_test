const initialState = {
  isFetching: true,
  data: [],
  error: null,
};


function phonebook(state = initialState, action){
    switch(action.type){
        default: 
        return state
    }
}

export default phonebook